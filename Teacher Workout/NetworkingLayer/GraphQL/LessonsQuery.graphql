query Lessons($themeId: ID) {
  lessons(themeId: $themeId) {
   edges {
      node {
        id,
        title,
        thumbnail {
          url,
          description
        }
        theme {
          title
        }
        duration {
            displayValue
        }
      }
    }
  }
}
