import SwiftUI

struct ProfileView: View {
    var body: some View {
        Color.blue
            .ignoresSafeArea()
    }
}

struct ProfileView_Previews: PreviewProvider {
    static var previews: some View {
        ProfileView()
    }
}
